# ── Core scraping & parsing ────────────────────────────────
beautifulsoup4
feedparser
trafilatura               # optional full-text extractor
playwright>=1.37.0        # HTML fallback

# ── Data / DB ───────────────────────────────────────────────
sqlalchemy>=2.0
python-dateutil
pytz
pytz-deprecation-shim     # only if you explicitly import it

# ── Networking & retries ───────────────────────────────────
requests
backoff
lxml_html_clean

# ── LLM & embeddings ───────────────────────────────────────
openai                    # via OpenRouter key
sentence-transformers
numpy

# ── Templating / e-mail ────────────────────────────────────
jinja2
premailer
python-slugify

# ── YouTube tools ──────────────────────────────────────────
google-api-python-client
youtube-transcript-api
yt-dlp                    # (only if you really need local audio)

# ── Utility ────────────────────────────────────────────────
python-dotenv
PyYAML

# ── Tor proxy support (optional) ───────────────────────────
stem                       # only if you actually use it
